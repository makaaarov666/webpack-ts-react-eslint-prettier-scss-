type TGeometryProps = {
  centerX: number;
  centerY: number;
  radius: number;
  angleInDegrees: number;
};

type TSegmentGeometry = {
  x: number;
  y: number;
};

export { TGeometryProps, TSegmentGeometry };
